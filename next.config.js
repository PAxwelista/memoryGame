/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: [],
    },

    presets: ["next/babel"],
};

module.exports = nextConfig;
